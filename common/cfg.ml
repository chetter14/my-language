/* def foo(x of string) of int
	y = (a <= b) && (a < c) || (b > c) && (a >= c);
	z = (a != b) || (a == c);
	w = a ^ b & c | d;
	v = (-a) && (!b || ~c);
end */

def foo (x of int) of char
	if (x == 2) then
		if (y == 10) then
			z = 1;
		else 
			z = 2;		
	else z = 2 * 3;
	
	z = 1000;
end